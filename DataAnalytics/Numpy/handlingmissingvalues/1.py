import numpy as np

#hadling missign values 

# np.isnan() # checks for nan values and returns a boolean array indicating where the nan values are located in the input array.
# np.isinf() # checks for inf values
# np.nan_to_num() # replaces nan with 0 and inf with a large number

