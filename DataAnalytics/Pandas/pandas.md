
##DataFrame Methods 

>Inplace Parameter: It is used to modify the dataframe directly and we do not need to create a new dataframe. If we don't use inplace parameter, then we need to create a new dataframe.

